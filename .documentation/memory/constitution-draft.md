# TriviaSpark Constitution (DRAFT)

> ⚠️ **DRAFT**: Generated by /speckit.discover-constitution on 2026-02-25
> Review carefully before finalizing. Run /speckit.constitution to formalize.

## Discovery Context

- **Source**: Analyzed 137 files in TriviaSpark repository
- **Patterns Found**: 15 high-confidence, 8 formalized via interactive questions
- **Coverage**: Frontend stack, backend stack, validation, testing, error handling, file organization, API architecture, code quality
- **Branch**: LastGood (default: main)

---

## Core Principles

### I. Frontend Technology Stack (MANDATORY)

All frontend code must use the standardized React 18+ stack for consistency, type safety, and maintainability.

**Required Technologies:**
- **React 19** with functional components and hooks (MUST)
- **TypeScript** with strict mode enabled (`strict: true` in tsconfig.json) (MUST)
- **Wouter** for client-side routing with proper base path handling (MUST)
- **TanStack Query** for server state management via `useQuery`/`useMutation` (MUST)
- **shadcn/ui** component library (new-york style) for all UI components (MUST)
- **Tailwind CSS** with wine-themed design system (`wine-*`, `champagne-*` tokens) (MUST)
- **Zod** + **react-hook-form** with `zodResolver` for all form validation (MUST)

**Code Splitting:**
- All page components MUST use `React.lazy()` for code splitting (MUST)
- Suspense boundaries MUST be implemented for lazy-loaded components (MUST)

**Evidence:**
- `client/src/App.tsx` - All pages lazy-loaded
- `tailwind.config.ts` - Wine/champagne color system defined
- `components.json` - shadcn/ui configured (new-york style)
- `tsconfig.json` - TypeScript strict mode enabled

**Rationale:** Ensures consistent developer experience, type safety, and optimized bundle sizes.

---

### II. Backend Technology Stack (MANDATORY)

All backend code must use the standardized ASP.NET Core + Entity Framework Core stack.

**Required Technologies:**
- **ASP.NET Core 9** (.NET 9) as the web framework (MUST)
- **Entity Framework Core** with SQLite for all data access (MUST)
- **C# nullable reference types** enabled (`<Nullable>enable</Nullable>`) (MUST)
- **Interface-based dependency injection** for all services (MUST)
- **Serilog** for structured logging (MUST)

**Database Requirements:**
- All data access MUST go through `TriviaSparkDbContext` (MUST)
- Production database path: `C:\websites\TriviaSpark\trivia.db` (MUST)
- Never create local database files in repository (MUST)
- Database schema MUST use snake_case for tables and columns (MUST)
- All entities MUST have data annotations (`[Key]`, `[Required]`, `[ForeignKey]`) (MUST)

**Service Layer Pattern:**
- Every service MUST have an `I*Service` interface (MUST)
- Services MUST be registered as `AddScoped` in DI container (MUST)
- All service methods MUST be async and return `Task<T>` (MUST)

**Evidence:**
- `TriviaSpark.Api/Program.cs` - EF Core + Serilog configuration
- `TriviaSpark.Api/Data/TriviaSparkDbContext.cs` - DbContext with snake_case mapping
- `TriviaSpark.Api/Services/EfCore/` - 13 services with interfaces
- `TriviaSpark.Api/TriviaSpark.Api.csproj` - Nullable enabled

**Rationale:** Provides type safety, testability through DI, and consistent data access patterns.

---

### III. Validation & Type Safety (MANDATORY)

All user input and data must be validated at multiple layers for security and data integrity.

**Frontend Validation:**
- All forms MUST use Zod schemas with `react-hook-form` + `zodResolver` (MUST)
- Shared types MUST be defined in `shared/schema.ts` using Zod (MUST)
- Client-side validation MUST provide immediate feedback (MUST)

**Backend Validation:**
- All entities MUST have data annotations (`[Required]`, `[StringLength]`, `[Range]`, etc.) (MUST)
- API endpoints MUST validate request inputs before processing (MUST)
- Validation errors MUST return structured 400 responses with field-level details (MUST)

**API Contracts:**
- JSON serialization MUST use camelCase for frontend compatibility (MUST)
- API responses MUST match TypeScript interfaces defined in shared schema (MUST)

**Evidence:**
- `client/src/pages/event-join.tsx` - Zod schema with min/max validation
- `shared/schema.ts` - Shared Drizzle + Zod schemas
- `TriviaSpark.Api/Data/Entities/Event.cs` - Data annotations on all fields
- `TriviaSpark.Api/Program.cs` - camelCase JSON configuration

**Rationale:** Defense in depth - catch errors early on client, enforce on server, prevent invalid data from reaching database.

---

### IV. Testing Standards (MANDATORY)

All new features must include automated tests to ensure code quality and prevent regressions.

**Test Requirements:**
- All new API endpoints MUST have integration tests using MSTest (MUST)
- All new frontend features MUST have unit tests using Vitest (MUST)
- Tests MUST pass before code can be merged to main branch (MUST)
- HTTP test files (.http) MUST be placed in `tests/http/` for manual testing (MUST)

**Test Organization:**
- Backend tests: MSTest project in solution
- Frontend tests: Vitest with React Testing Library
- Integration tests: Test against real SQLite database
- HTTP tests: `tests/http/*.http` for manual API verification

**Coverage Goals:**
- Critical business logic: 80% minimum coverage
- API endpoints: 100% of happy paths, key error scenarios
- Frontend forms: All validation scenarios

**Current State:**
- 0 automated tests exist (technical debt)
- 25 HTTP test files for manual testing
- Testing must be incrementally added to new features

**Evidence:**
- `tests/http/` - 25 HTTP test files exist
- No test framework configured yet (gap to address)

**Rationale:** Automated tests prevent regressions, enable confident refactoring, and serve as living documentation.

---

### V. Error Handling & Observability (MANDATORY)

All errors must be handled gracefully and logged comprehensively for debugging and monitoring.

**Exception Handling:**
- All API requests MUST flow through `ExceptionHandlingMiddleware` (MUST)
- Exceptions MUST be mapped to appropriate HTTP status codes (400, 401, 404, 500) (MUST)
- Error responses MUST include `requestId`, `timestamp`, `path`, `method` (MUST)
- Sensitive details MUST NOT be exposed in production error messages (MUST)

**Structured Logging:**
- All backend operations MUST use `ILoggingService` wrapper (MUST)
- Log appropriate events: `LogApiCall`, `LogBusinessEvent`, `LogError`, `LogPerformance`, `LogDatabaseOperation` (MUST)
- Serilog MUST be configured with console + file sinks (MUST)
- Separate error log file MUST capture all errors and warnings (MUST)
- Performance warnings MUST be logged for operations >1s (MUST)

**Frontend Error Handling:**
- React Error Boundaries MUST be used for component error isolation (MUST)
- NO `console.log` statements in production code (MUST)
- User-facing errors MUST be displayed via toast notifications (MUST)

**Evidence:**
- `TriviaSpark.Api/Middleware/ExceptionHandlingMiddleware.cs` - Centralized exception handling
- `TriviaSpark.Api/Services/LoggingService.cs` - Structured logging wrapper
- `TriviaSpark.Api/appsettings.json` - Serilog with file/console sinks
- `client/src/App.tsx` - Some console.log found (cleanup needed)

**Rationale:** Proper error handling improves user experience and makes production issues diagnosable.

---

### VI. File Organization & Repository Structure (MANDATORY)

All files must be placed in designated directories to maintain clean, navigable repository structure.

**Directory Structure:**
```
TriviaSpark/
├── client/src/          # Frontend React application
│   ├── components/      # Reusable UI components
│   │   ├── ui/         # shadcn/ui components (49 files)
│   │   └── [feature]/  # Feature-specific components
│   ├── pages/          # Page components (must be lazy-loaded)
│   ├── hooks/          # Custom React hooks
│   ├── contexts/       # React context providers
│   └── lib/            # Utilities and configurations
├── TriviaSpark.Api/     # ASP.NET Core backend
│   ├── Controllers/    # API controllers (Minimal API preferred)
│   ├── Services/       # Business logic services
│   ├── Data/           # EF Core DbContext and entities
│   ├── Middleware/     # Request/response middleware
│   └── Utils/          # Backend utilities
├── shared/              # Shared TypeScript schemas
├── tests/               # All testing files
│   └── http/           # HTTP test files (ALL .http files)
├── tools/               # Development and test scripts
├── copilot/             # Generated documentation
├── temp/                # Temporary files (gitignored)
└── [config files]       # Root reserved for config only
```

**Placement Rules:**
- Development/test scripts → `tools/` (MUST)
- **ALL** .http files → `tests/http/` (MUST)
- Generated documentation → `copilot/` (MUST)
- Temporary/cache files → `temp/` (MUST)
- Source code → `client/src/` or `TriviaSpark.Api/` (MUST)
- Root directory: ONLY configuration, README, LICENSE, solution files (MUST)

**Evidence:**
- `.github/copilot-instructions.md` - Extensive file organization rules
- All 137 source files follow this pattern
- 25 HTTP files correctly located in `tests/http/`

**Rationale:** Clean organization makes codebase navigable, prevents clutter, and enforces separation of concerns.

---

### VII. API Architecture Pattern (MANDATORY)

All API endpoints must follow a consistent architectural pattern for maintainability.

**Preferred Pattern: Minimal API**
- New API endpoints SHOULD use Minimal API style (inline `app.MapGet()`, `app.MapPost()`, etc.) (SHOULD)
- Endpoints MUST be grouped logically in `ApiEndpoints.EfCore.cs` or feature-specific files (MUST)
- Complex endpoints with extensive logic MAY use Controllers when Minimal API becomes unwieldy (MAY)

**Migration Path:**
- Existing Controllers (`EventsV2Controller`, etc.) should be gradually migrated to Minimal API
- Controllers are acceptable during transition period
- New features should prefer Minimal API unless complexity requires Controllers

**API Versioning:**
- Use route prefixes for versioning: `/api/`, `/api/v2/` (SHOULD)
- Document breaking changes in CHANGELOG when introducing new versions (MUST)

**Route Organization:**
- Group related endpoints: `/api/events`, `/api/events/{id}`, `/api/events/{id}/teams` (MUST)
- Use proper HTTP verbs: GET (read), POST (create), PUT (replace), PATCH (update), DELETE (remove) (MUST)
- Return appropriate status codes: 200 (OK), 201 (Created), 204 (No Content), 400 (Bad Request), 404 (Not Found) (MUST)

**Evidence:**
- `TriviaSpark.Api/ApiEndpoints.EfCore.cs` - Primary Minimal API endpoints
- `TriviaSpark.Api/Controllers/` - 4 controllers (mid-migration state)

**Rationale:** Minimal API reduces boilerplate, improves readability, and keeps related endpoints together.

---

### VIII. Code Quality & Documentation (MANDATORY)

All code must be documented and formatted consistently for maintainability.

**C# Documentation:**
- All public classes, interfaces, and methods MUST have XML `<summary>` comments (MUST)
- Complex algorithms MUST include explanatory comments (MUST)
- API controllers/endpoints MUST document parameters and return types (MUST)

**TypeScript Documentation:**
- Component props MUST use TypeScript interfaces (serves as documentation) (MUST)
- Complex business logic SHOULD include explanatory comments (SHOULD)
- Public utility functions MAY use JSDoc for additional context (MAY)

**Code Formatting:**
- ESLint MUST be configured for TypeScript with recommended rules (MUST)
- Prettier MUST be configured for consistent code formatting (MUST)
- Pre-commit hooks SHOULD enforce linting and formatting (SHOULD)
- Continuous Integration SHOULD run linting checks (SHOULD)

**Configuration Requirements:**
- `.eslintrc.json` MUST be added with TypeScript + React rules (MUST)
- `.prettierrc` MUST be added with project formatting standards (MUST)
- `.husky/` pre-commit hooks SHOULD run lint and format checks (SHOULD)

**Evidence:**
- 73% of C# public methods have XML docs (good baseline)
- TypeScript components use interface-based props
- NO ESLint or Prettier configuration found (gap to address)
- NO pre-commit hooks configured (gap to address)

**Rationale:** Consistent formatting reduces cognitive load; documentation helps onboarding and maintenance.

---

## Additional Standards

### Security Best Practices

**Authentication:**
- Current state: Authentication removed (anonymous mode)
- Future: When auth is re-added, MUST use secure session management with HTTP-only cookies (MUST)
- Password hashing MUST use BCrypt with appropriate work factor (MUST)

**Input Sanitization:**
- All user input MUST be validated and sanitized (MUST)
- SQL injection prevention via EF Core parameterized queries (MUST)
- XSS prevention via React's automatic escaping + CSP headers (MUST)

**API Security:**
- CORS MUST be properly configured with specific origins in production (MUST)
- Rate limiting SHOULD be implemented on public endpoints (SHOULD)
- API keys for external services (OpenAI, Unsplash) MUST be stored in User Secrets or environment variables (MUST)

### Performance Standards

**Build Process:**
- Frontend build MUST output to `TriviaSpark.Api/wwwroot` for integrated deployment (MUST)
- Static build for GitHub Pages MUST output to `docs/` directory (MUST)
- Build warnings SHOULD be addressed and not ignored (SHOULD)

**Runtime Performance:**
- Database queries MUST use appropriate indexes (MUST)
- EF Core SHOULD use `.AsNoTracking()` for read-only queries (SHOULD)
- Frontend SHOULD use `React.memo()` for expensive components (SHOULD)

**Deployment:**
- Always build frontend: `npm run build` (MUST)
- Always run backend: `dotnet run --project ./TriviaSpark.Api/TriviaSpark.Api.csproj` (MUST)
- Never use Vite dev server in production workflows (MUST)

---

## Governance

### Amendment Process
- Constitution amendments require team discussion and approval
- Breaking changes MUST be documented in CHANGELOG
- Migration plans MUST be provided for all breaking changes
- Reviews should occur quarterly to ensure constitution stays current

### Compliance
- All Pull Requests MUST verify compliance with core principles
- Spec Kit agents (`/speckit.pr-review`) SHOULD be used for automated compliance checks
- Technical debt exceptions MAY be granted with explicit documentation and repayment plan

### Living Document
- This constitution supersedes informal practices
- Conflicts between constitution and existing code should be resolved toward constitution
- Constitution evolves with project needs but changes require team consensus

---

**Version**: 0.1.0-draft  
**Generated**: 2026-02-25  
**Discovered From**: 137 source files, 15 high-confidence patterns, 8 interactive decisions  
**Status**: DRAFT - Requires team review and approval
